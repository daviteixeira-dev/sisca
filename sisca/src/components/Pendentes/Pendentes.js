import React from "react";
import './Pendentes.css';

const Pendentes = () => {
    return(
        <section className="Pendentes">
            <h1>Pendentes</h1>
        </section>
    );
}

export default Pendentes;